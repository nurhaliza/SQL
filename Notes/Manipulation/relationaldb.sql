/* == manipulation - relational db == */
/*
return info from rel db
- rel db = db that organizes info into 1+ tables
- table (relation) = collection of data organized into rows + columns 
- column = set of data values or a particular type
- row = single record in a table 
- all data stored in a rel db is of a certain data type
ex: INTEGER (+ or - whole #), TEXT (string), DATE (YYYY-MM-DD), REAL (decimal value)
*/
SELECT * FROM celebs; 